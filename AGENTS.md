# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Agent workflow

When the user asks for a change, **always commit and push to GitHub** (`git push origin main` or the feature branch) before finishing — even if the code change is small. Deploy to Railway when the change affects what they run in Expo Go.
