[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh scripts/railway-entrypoint.sh"
healthcheckPath = "/status"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
