[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "/bin/sh -c 'node scripts/ping-brrr.mjs || true'"
startCommand = "npm run start:railway"
healthcheckPath = "/status"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
